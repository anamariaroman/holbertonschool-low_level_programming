#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *  main - Alphabetic
 *
 *  Description: Use for
 *  Return: void
 */

int main(void)
{
	int ch;

	for (ch = 97; ch <= 122; ch++)
		putchar(ch);

	for (ch = 65; ch <= 90; ch++)
		putchar(ch);

	putchar('\n');
	return (0);
}
