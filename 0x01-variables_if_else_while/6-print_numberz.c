#include <stdio.h>

/**
 * main - print numbers (0 - 10) without char
 *
 * Return: void
 */

int main(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
	}
	putchar(10);
	return (0);
}
